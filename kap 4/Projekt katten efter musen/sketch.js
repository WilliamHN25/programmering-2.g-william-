let x=random(0,windowWidth)
let y=random(0,windowHeight)
let a=speed
let b=0
let d=20
let speed=10


function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  speed;
  circle(x,y,d)
}
